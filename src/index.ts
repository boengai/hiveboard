import { consola } from "consola";

// NOTE: The legacy Orchestrator (src/orchestrator/) has been removed.
// This entry point is superseded by packages/api/src/orchestrator/.
// Update this file to wire up the new API package orchestrator.

consola.warn("src/index.ts: legacy entry point — wire up packages/api orchestrator here");

